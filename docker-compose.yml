version: '2'
services:
    web: 
        build: .
        ports: 
            - "5000:5000"
        volumes:
            - .:/usr/src/app
        depends_on: 
            - mongo
    
    mongo:
        image: mongo
